#ifndef BCAIA_TYPES_H
#define BCAIA_TYPES_H

// Included automatically at the top of the auto-generated RcppExports.cpp so
// that the unqualified Armadillo types (mat, vec, rowvec, uvec, cube) used in
// the exported function signatures resolve correctly.
#include <RcppArmadillo.h>
using namespace arma;

#endif
