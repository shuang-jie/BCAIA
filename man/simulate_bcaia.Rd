% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_bcaia}
\alias{simulate_bcaia}
\title{Simulate data from the covariate-varying interaction model (Simulation 1)}
\usage{
simulate_bcaia(n = 30, J = 15, K_true = 2, sigma = 0.5, seed = NULL)
}
\arguments{
\item{n}{Number of samples (must be a multiple of 6). Default \code{30}.}

\item{J}{Number of features. Default \code{15}.}

\item{K_true}{True number of covariate-varying factors. Default \code{2}.}

\item{sigma}{True idiosyncratic standard deviation. Default \code{0.5}.}

\item{seed}{Optional integer seed.}
}
\value{
A list with the count matrix \code{Y}, the design matrices
  \code{Xmean} and \code{Xcov}, and a \code{truth} sublist containing
  \code{Sigma} (a \code{J x J x n} array), \code{Q}, \code{F}, \code{beta},
  \code{ri}, \code{alpha} and \code{mu}.
}
\description{
Generates a dataset mirroring Simulation 1 of the paper: a categorical
design with two factors (a binary and a ternary covariate) giving six
experimental conditions, a rank-\code{K_true} covariate-varying covariance,
and mean-abundance regression coefficients. Useful for quick demonstrations
and for reproducing the simulation study.
}
\examples{
sim <- simulate_bcaia(seed = 6)
dim(sim$Y)
}
