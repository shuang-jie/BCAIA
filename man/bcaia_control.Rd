% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bcaia_control.R
\name{bcaia_control}
\alias{bcaia_control}
\title{Hyperparameter and MCMC control settings for \code{\link{bcaia}}}
\usage{
bcaia_control(
  a_phi = 1/20,
  a_tau = 0.1,
  b_tau = NULL,
  a_sig = 3,
  b_sig = 3,
  u2_beta = 1,
  Lr = 30,
  L_alpha = 35,
  a_psi_r = 1,
  a_psi_alpha = 1,
  a_w = 5,
  b_w = 5,
  a_w_alpha = 5,
  b_w_alpha = 5,
  ur2 = 1,
  acc_tar = 0.234
)
}
\arguments{
\item{a_phi, a_tau, b_tau}{Dirichlet-Horseshoe hyperparameters for the factor
loadings. Defaults \code{a_tau = 0.1}, \code{b_tau = 1/J} (set internally
from the data when \code{NULL}) and \code{a_phi = 1/20}.}

\item{a_sig, b_sig}{Inverse-gamma prior parameters for the idiosyncratic
variance \eqn{\sigma^2}. Default \code{3} and \code{3}.}

\item{u2_beta}{Prior variance of the mean-regression coefficients
\eqn{\beta_{jp}}. Default \code{1}.}

\item{Lr, L_alpha}{Stick-breaking truncation levels for the size factor
\eqn{r} and baseline abundance \eqn{\alpha}. Defaults \code{30} and
\code{35}.}

\item{a_psi_r, a_psi_alpha}{Dirichlet-process concentration parameters for
\eqn{r} and \eqn{\alpha}. Default \code{1} (the subject-indexed models in
the paper use \code{3}).}

\item{a_w, b_w, a_w_alpha, b_w_alpha}{Beta prior parameters for the inner
mixture weights. Default \code{5} each.}

\item{ur2}{Prior variance of the size factor \eqn{r}. Default \code{1}.}

\item{acc_tar}{Target acceptance rate for the adaptive Metropolis-Hastings
updates. Default \code{0.234}.}
}
\value{
A named list of class \code{"bcaia_control"}.
}
\description{
Collects the fixed hyperparameters of the model together with the
Metropolis-Hastings tuning constants into a single list. All values have
defaults matching those used in the simulation studies of Zhang, Patnode and
Lee; override individual entries by passing them as named arguments.
}
\examples{
ctrl <- bcaia_control(a_psi_r = 3, a_psi_alpha = 3)
}
